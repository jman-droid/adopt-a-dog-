function showInfo(name, breed,fee) {
  alert(' Name:'+ name + ' Breed:'+ breed + ' Fee: $'+ fee);
}

let fee = 123.45;
let total = 0;

function totalAdder(fee) {

  total = fee + total;

  alert(total);


}
